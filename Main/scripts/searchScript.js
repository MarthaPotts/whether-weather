//search history 
    //localStorage
    let getData = JSON.parse(localStorage.getItem()); 
    let setData = JSON.stringify(localStorage.setItem()); 

    //doublecheck the order of the scripts: search may need to be first 
    